name: CI/CD Pipeline

on:
  push:
    branches: [ main, ci-cd-pipeline ]
  pull_request:
    branches: [ main, ci-cd-pipeline ]

permissions:
  contents: write
  actions: write

jobs:
  matrix:
    name: Matrix Definitions
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.set-matrix.outputs.result }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Detect Changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            poexy-core:
              - 'tools/poexy-core/**'
            ignite:
              - 'tools/ignite/**'

      - name: Set Matrix
        id: set-matrix
        env:
          POEXY_CORE_PROJECT_CHANGED: ${{ steps.filter.outputs.poexy-core }}
          IGNITE_PROJECT_CHANGED: ${{ steps.filter.outputs.ignite }}
        shell: bash
        run: python .github/workflows/projects_matrix.py

  poetry-cicd:
    name: Poetry CI/CD - ${{ matrix.project.name }}
    needs: [matrix]
    strategy:
      matrix: ${{ fromJSON(needs.matrix.outputs.result) }}
    uses: ./.github/workflows/poetry-cicd.yml
    with:
        supported-os: ${{ matrix.project.inputs.supported-os }}
        supported-python-versions: ${{ matrix.project.inputs.supported-python-versions }}
        last-supported-python-version: ${{ matrix.project.inputs.last-supported-python-version }}
        pipx-tools: ${{ matrix.project.inputs.pipx-tools }}
        poetry-project-path: ${{ matrix.project.inputs.path }}
        poetry-package-name: ${{ matrix.project.inputs.package-name }}
        poetry-dependency-groups: ${{ matrix.project.inputs.dependency-groups }}
        builds-registry-path: ${{ matrix.project.inputs.builds-registry-path }}
        builds-registry-key: ${{ matrix.project.inputs.builds-registry-key }}
        use-poexy-core: ${{ matrix.project.inputs.use-poexy-core == 'true' }}
        use-mutex: ${{ matrix.project.inputs.use-mutex == 'true' }}
        is-single-artifact: ${{ matrix.project.inputs.is-single-artifact == 'true' }}
    secrets:
      database-connection-uri: ${{ secrets.POSTGRESQL_CONNECTION_URI }}
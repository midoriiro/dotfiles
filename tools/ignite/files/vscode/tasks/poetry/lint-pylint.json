{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "${project-name}: Run Lint (pylint)",
            "type": "shell",
            "command": "poetry run pylint",
            "problemMatcher": [
                "$python"
            ]
        }
    ]
}
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "${projectName}: Run Lint (pylint)",
            "type": "shell",
            "command": "poetry run pylint",
            "problemMatcher": [
                "$python"
            ]
        }
    ]
}
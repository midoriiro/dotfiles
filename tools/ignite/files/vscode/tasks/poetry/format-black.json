{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "${project-name}: Run Format (black)",
            "type": "shell",
            "command": "poetry run black",
            "problemMatcher": [
                "$python"
            ]
        }
    ]
}
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "${projectName}: Install",
            "type": "shell",
            "command": "poetry install",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
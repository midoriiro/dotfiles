[project]
name = "missing-readme-file"
version = "1.0.0"
readme = { file = "README.md", content-type = "text/markdown" }

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.missing_readme_file]
source = "src"

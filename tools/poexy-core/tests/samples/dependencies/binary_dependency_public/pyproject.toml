[project]
name = "binary-dependency-public"
version = "1.0.0"
dependencies = [
    "typer>=0.16.0",
]

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.binary-dependency-public]
source = "src"

[tool.poexy.wheel]
format = ["binary"]

[tool.poexy.binary]
name = "binary-dependency-public"
entry_point = "src.__init__"
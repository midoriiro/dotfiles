[project]
name = "binary-dependency-vcs"
version = "1.0.0"
dependencies = [
    "library @ git://localhost:8001/library.git"
]

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.binary-dependency-vcs]
source = "src"

[tool.poexy.wheel]
format = ["binary"]

[tool.poexy.binary]
name = "binary-dependency-vcs"
entry_point = "src.__init__"
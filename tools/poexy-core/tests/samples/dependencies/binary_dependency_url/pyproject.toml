[project]
name = "binary-dependency-url"
version = "1.0.0"
dependencies = [
    "library @ http://localhost:8000/library-1.0.0-py3-none-any.whl"
]

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.binary-dependency-url]
source = "src"

[tool.poexy.wheel]
format = ["binary"]

[tool.poexy.binary]
name = "binary-dependency-url"
entry_point = "src.__init__"
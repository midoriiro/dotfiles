[project]
name = "symbolic-link-point-to-absolute-file"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_point_to_absolute_file]
source = "src"

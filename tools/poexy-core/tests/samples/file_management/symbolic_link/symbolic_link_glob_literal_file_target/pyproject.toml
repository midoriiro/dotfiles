[project]
name = "symbolic-link-glob-literal-file-target"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_glob_literal_file_target]
source = "src"
includes = [
    "src/symlink"
]

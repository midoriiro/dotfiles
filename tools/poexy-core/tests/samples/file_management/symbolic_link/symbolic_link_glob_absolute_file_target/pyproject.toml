[project]
name = "symbolic-link-glob-absolute-file-target"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_glob_absolute_file_target]
source = "src"
includes = [
    { path = "symlink*" }
]

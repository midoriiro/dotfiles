[project]
name = "symbolic-link-points-to-directory"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_points_to_directory]
source = "src"

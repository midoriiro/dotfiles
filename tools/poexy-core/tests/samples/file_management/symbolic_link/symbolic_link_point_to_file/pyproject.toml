[project]
name = "symbolic-link-point-to-file"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_point_to_file]
source = "src"

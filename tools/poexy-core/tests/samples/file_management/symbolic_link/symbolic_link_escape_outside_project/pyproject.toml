[project]
name = "symbolic-link-escape-outside-project"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_escape_outside_project]
source = "src"

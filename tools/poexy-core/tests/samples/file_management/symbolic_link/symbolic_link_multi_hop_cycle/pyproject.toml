[project]
name = "symbolic-link-multi-hop-cycle"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_multi_hop_cycle]
source = "src"

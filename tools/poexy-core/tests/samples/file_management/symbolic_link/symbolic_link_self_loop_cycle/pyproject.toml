[project]
name = "symbolic-link-self-loop-cycle"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.symbolic_link_self_loop_cycle]
source = "src"

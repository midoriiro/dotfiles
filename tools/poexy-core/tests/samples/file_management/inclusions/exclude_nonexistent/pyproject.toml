[project]
name = "exclude-nonexistent"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.exclude_nonexistent]
source = "src"
excludes = [
    "**/nonexistent/*"
]

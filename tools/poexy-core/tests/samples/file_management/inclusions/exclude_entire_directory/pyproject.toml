[project]
name = "exclude-entire-directory"
version = "1.0.0"

[build-system]
requires = ["poexy-core"]
build-backend = "poexy_core.api"

[tool.poexy.package.exclude_entire_directory]
source = "src"
excludes = [
    "src/resources"
]
